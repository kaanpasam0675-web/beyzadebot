# Nova Bot - Linux (Oracle Cloud / herhangi bir Ubuntu VPS) bağımlılıkları
# NOT: davey Windows-only bir modüldür, Linux'ta kurulmaz. Ses, standart
# discord.py VoiceClient + PyNaCl ile çalışır.
discord.py[voice]>=2.3.2
Flask>=3.0.0
python-dotenv>=1.0.0
aiohttp>=3.9.0
